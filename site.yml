---

- name: first playbook - nginx
  hosts: all
  become: true
  gather_facts: true
  pre_tasks:

  - name: update repository index
    tags: always
    apt:
      update_cache: yes
    changed_when: false

- hosts: all
  become: true
  gather_facts: true
  roles:
    - nginx
